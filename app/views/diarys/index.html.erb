<% content_for :page_title, "Index"%>

<h1 class="title"> Diary index<h1>
<section class="section">
    <div class="container">.
        <% @diarys.each do |diary| %>
        <div class="card">
		  <div class="card-content">
		    <div class="media">
		      <div class="media-content">
		        <p class="title is-4"><%= link_to diary.title, diary  %></p>
		      </div>
		    </div>
		    <div class="content">
		     	<%= diary.expiration %>
                <%= diary.kind %>
		    </div>
		  </div>
		</div>
            
        <% end %>
    </div>
</section>